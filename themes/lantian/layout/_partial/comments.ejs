<% if (page_language() == 'zh') { %>
<p class="text-center">本站使用运行在 Vercel 上的 Waline 评论系统，中国大陆访问可能不稳定。</p>
<% } %>

<div id="waline"></div>
<script>
    var waline_config = {
        el: '#waline',
        serverURL: 'https://comments.lantian.pub',
        path: '/<%= remove_trailing_slash(page.path) %>',
        lang: '<% if (page_language() == "zh") { %>zh-CN<% } else { %>en-US<% } %>',
        requiredMeta: ['nick', 'mail'],
        emoji: [
            'https://cdn.jsdelivr.net/gh/walinejs/emojis@1.0.0/tieba',
            'https://cdn.jsdelivr.net/gh/walinejs/emojis@1.0.0/bilibili',
        ],
    };
</script>
